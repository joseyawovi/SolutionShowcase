{
  "name": "solution-showcase-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd client && npm install && npm run dev",
    "build": "cd client && npm install && npm run build",
    "preview": "cd client && npm run preview"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}
